# 📂 คู่มือโครงสร้างโฟลเดอร์สำหรับ Roblox (Rojo Standard)

การจัดวางโครงสร้างไฟล์ที่เป็นมาตรฐานจะช่วยให้โปรเจกต์สเกลได้ง่าย ทำงานร่วมกันเป็นทีมได้ดี และลดปัญหาเรื่องหาไฟล์ไม่เจอในระยะยาว สำหรับโปรเจกต์ Roblox ที่ใช้ **Rojo** โครงสร้างที่ได้รับความนิยมสูงสุดคือการแบ่งตาม **Service/Controller Pattern** (คล้ายโครงสร้าง Knit Framework)

---

## 🏗️ โครงสร้างโฟลเดอร์โดยรวม (Directory Structure)

```text
src/
 ├─ server/               👉 ซิงก์ไปที่ ServerScriptService (ทำงานฝั่งเซิร์ฟเวอร์เท่านั้น)
 │   ├─ services/         👉 ระบบหลักของเกม (เช่น DataService, CombatService)
 │   ├─ components/       👉 ระบบที่ผูกติดกับ Object (เช่น DoorComponent, ZombieComponent)
 │   └─ ServerMain.server.lua 👉 จุดเริ่มต้นการทำงานฝั่งเซิร์ฟเวอร์ (Entry Point)
 │
 ├─ client/               👉 ซิงก์ไปที่ StarterPlayerScripts (ทำงานฝั่งผู้เล่นเท่านั้น)
 │   ├─ controllers/      👉 ระบบจัดการฝั่ง Client (เช่น CameraController, InputController)
 │   ├─ components/       👉 เอฟเฟกต์ภาพ/เสียงที่ผูกติดกับ Object ฝั่ง Client
 │   ├─ ui/               👉 โค้ดที่ใช้ควบคุมหน้าต่าง UI ต่างๆ
 │   └─ ClientMain.client.lua 👉 จุดเริ่มต้นการทำงานฝั่ง Client (Entry Point)
 │
 └─ shared/               👉 ซิงก์ไปที่ ReplicatedStorage (แชร์ใช้งานได้ทั้ง Server และ Client)
     ├─ constants/        👉 เก็บค่าคงที่ของเกม (เช่น ตั้งค่าไอเทม, ราคาของ, สี UI)
     ├─ modules/          👉 Utility Functions/Classes ที่ใช้ได้ทั้งสองฝั่ง (เช่น MathUtil, FormatUtil)
     ├─ network/          👉 จัดการ RemoteEvents / RemoteFunctions
     └─ types/            👉 ไฟล์เก็บ Type Definitions (Luau Types) สำหรับอ้างอิง
```

---

## 📝 รายละเอียดแต่ละส่วน (Deep Dive)

### 1. `src/server/` (ฝั่งเซิร์ฟเวอร์ - ระบบหลังบ้าน)
โค้ดที่อยู่ในโฟลเดอร์นี้จะปลอดภัยจากการถูกผู้เล่นมองเห็นหรือแฮ็ก ควรใช้จัดการตรรกะสำคัญของเกมทั้งหมด
* **`services/`**: เป็น ModuleScript ที่มีหน้าที่ดูแลระบบใดระบบหนึ่งโดยเฉพาะอย่างเบ็ดเสร็จ เช่น 
  * `DataService.lua` (จัดการโหลด/เซฟข้อมูลผู้เล่น)
  * `EconomyService.lua` (จัดการเงิน/ธุรกรรมต่างๆ)
  * *ข้อควรระวัง: ไม่ควรให้ Service อ้างอิงกันเองเป็นวงกลม (Circular Dependency)*
* **`ServerMain.server.lua`**: มีหน้าที่เพียงแค่ Require และ Start Services ทุกตัวขึ้นมาทำงานเมื่อเซิร์ฟเวอร์เปิด

### 2. `src/client/` (ฝั่งไคลเอนต์ - ระบบหน้าบ้าน)
โค้ดที่รันบนเครื่องของผู้เล่นโดยตรง ใช้สำหรับรับ Input ขยับกล้อง หรือแสดงผลภาพ/เสียง/UI ให้สวยงาม
* **`controllers/`**: ทำหน้าที่คล้าย Service แต่เป็นของฝั่ง Client เช่น
  * `MovementController.lua` (ควบคุมการเคลื่อนไหวพิเศษ เช่น พุ่ง, กลิ้งหลบ)
  * `AudioController.lua` (เล่นเสียงดนตรีประกอบ หรือ SFX)
* **`ui/`**: โฟลเดอร์รวมโค้ดจัดการหน้าจอ มักจะแบ่งย่อยเป็น `components/` (ปุ่ม, หลอดเลือด) และ `views/` (หน้าต่างร้านค้า, หน้าตั้งค่า)

### 3. `src/shared/` (ส่วนกลาง)
โค้ดที่สามารถถูกเรียกใช้งานได้จากทั้งฝั่ง Server และ Client ช่วยลดการเขียนโค้ดซ้ำซ้อน
* **`constants/`**: ไฟล์ Config สำคัญ เช่น `GameConfig.lua` หรือ `ItemData.lua` (เมื่อต้องการปรับสมดุลเกม หรือแก้ราคาดาบ ให้มาแก้ที่โฟลเดอร์นี้ที่เดียว)
* **`network/`**: โมดูลสำหรับจัดการเรื่องการสื่อสารระหว่าง Client-Server มักใช้เชื่อมต่อกับ RemoteEvent ให้เรียกใช้ง่ายขึ้น เป็นระเบียบ ไม่ต้องสร้าง Event เองใน Studio

---

## 🌍 โครงสร้างฝั่ง Roblox Studio (DataModel Hierarchy)
แม้ว่าเราจะจัดโครงสร้างสคริปต์ในโฟลเดอร์ฝั่งคอมพิวเตอร์ (VSCode/Rojo) ได้ดีแล้ว แต่ **การจัดระเบียบ 3D Model, UI และ Asset ต่างๆ ในโปรแกรม Roblox Studio** ก็สำคัญไม่แพ้กัน เพื่อความเป็นระเบียบและง่ายต่อการเขียนสคริปต์เรียกใช้ ควรจัดวางตามมาตรฐานดังนี้:

### 1. `Workspace` (ฉากและสิ่งของที่แสดงผลอยู่ปัจจุบัน)
ใช้สำหรับวางของที่ผู้เล่นต้องเห็นและโต้ตอบได้ **ห้าม**วางของสะเปะสะปะ ควรจัดกลุ่ม (Group) ใส่โฟลเดอร์ให้ชัดเจน:
* `Workspace/Maps/`: ด่านหรือแผนที่ที่กำลังเล่นอยู่
* `Workspace/Lobby/`: โซนจุดรอเริ่มเกม
* `Workspace/Spawns/`: จุดเกิด (SpawnLocations) เพื่อให้สคริปต์วนลูปหาได้ง่าย
* `Workspace/Temp/` หรือ `Workspace/ActiveItems/`: โฟลเดอร์เปล่าสำหรับใช้เก็บของที่เสกขึ้นมาระหว่างเล่น (เช่น กระสุนปืน, ปืนที่ดรอปพื้น) เพื่อให้ลบทิ้งได้ง่ายตอนจบตา

### 2. `ServerStorage` (คลังเก็บของลับฝั่งเซิร์ฟเวอร์)
**ผู้เล่นจะมองไม่เห็นและแฮ็กเข้าโฟลเดอร์นี้ไม่ได้เด็ดขาด** ใช้สำหรับเก็บโมเดลหรือด่านที่ยังไม่ใช้งาน แต่รอให้โค้ด (Server) ดึงไปวางในฉากทีหลัง
* `ServerStorage/Maps/`: เก็บด่านทั้งหมด (เช่น Map_City, Map_Forest) สคริปต์จะโคลน (Clone) จากที่นี่ไปวางใน Workspace
* `ServerStorage/ServerModels/`: โมเดลพิเศษที่ยังไม่อยากให้ฝั่งไคลเอนต์เห็นโหลดไว้ล่วงหน้า

### 3. `ReplicatedStorage` (คลังเก็บของส่วนกลาง)
ของที่อยู่ในนี้ **ผู้เล่น (Client) จะมองเห็นและโหลดไว้ล่วงหน้า (Preload)** เหมาะสำหรับเก็บของที่ฝั่ง Client ต้องเรียกไปใช้บ่อยๆ
* `ReplicatedStorage/Assets/Weapons/`: โมเดลปืนและมีด (เพราะฝั่ง Client ต้องดึงไปทำ Animation หรือใส่ในมือ)
* `ReplicatedStorage/Assets/VFX/`: เอฟเฟกต์แสง สี เสียง (Particle, Sound)
* `ReplicatedStorage/Assets/UI_Templates/`: ต้นแบบหน้าจอ UI เผื่อให้สคริปต์ก๊อปปี้ไปแสดงผล

### 4. `StarterGui` และโฟลเดอร์อื่นๆ
* `StarterGui/`: ถ้าจะจัดวาง UI แบบแมนนวล (ไม่อยากเขียนโค้ดสร้าง UI) ให้จัดโฟลเดอร์ย่อยในนี้ให้เป็นระเบียบ เช่น `StarterGui/HUD`, `StarterGui/Menus`

---

## 🎯 กฎเหล็กในการพัฒนา (Best Practices)

1. **Single Responsibility Principle (SRP):** 1 ไฟล์ ควรมีหน้าที่เดียวอย่างชัดเจน อย่าเอาโค้ดทุกระบบไปยัดรวมใน `ServerMain.lua`
2. **Never Trust the Client (อย่าไว้ใจฝั่งไคลเอนต์):** โค้ดฝั่ง `client/` สามารถถูกแก้ไข/ดักจับได้โดย Hacker ดังนั้น การทำงานสำคัญ เช่น การซื้อของ, ลดเลือด, หรือสุ่มของรางวัล **ต้อง**ให้ฝั่งไคลเอนต์ส่งคำขอ (FireServer) ไปให้ `server/` เป็นคนคำนวณและตรวจสอบความถูกต้องเสมอ
3. **ใช้ ModuleScript เป็นหลัก:** โปรเจกต์ที่สเกลได้ดีควรมี `Script` (.server.lua) และ `LocalScript` (.client.lua) แค่อย่างละ 1-2 ไฟล์เท่านั้น (ใช้เป็น Entry Point) ส่วนระบบย่อยอื่นๆ ให้เขียนเป็น `ModuleScript` แล้วใช้ `require()` เอา
4. **กำหนด Naming Convention ให้ชัดเจน:**
   * สคริปต์หลักใช้ PascalCase: `PlayerManager.lua`
   * ตัวแปร/ฟังก์ชันทั่วไปใช้ camelCase: `local maxHealth = 100`
   * ค่าคงที่ (Constants) ใช้ UPPER_SNAKE_CASE: `local MAX_PLAYERS = 50`

---
💡 **Tips:** โครงสร้างนี้สอดคล้องกับมาตรฐานของนักพัฒนา Roblox มืออาชีพและทักษะ `roblox-game-development` ที่คุณมีครับ การใช้โครงสร้างแบบนี้จะทำให้ทั้งทีมงาน และ AI Agent สามารถทำความเข้าใจ วางแผน และเขียนโค้ดต่อยอดได้รวดเร็วและแม่นยำขึ้นอย่างมาก
